"""Command-line entry point for the SplitEdge batch importer."""


def run() -> str:
    """Return the current foundation status until Milestone 1 adds ingestion."""
    return "SplitEdge importer foundation ready"


if __name__ == "__main__":
    print(run())
